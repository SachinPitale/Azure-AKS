apiVersion: v1 # String
kind: Pod # String
metadata: # Dictionary
  name: myapp-pod
  labels: # Dictionary
    app: myapp  # Key value paids
spec:
  containers: # List
    - name: myapp
      image: sachinpitale22/mynginx_image1:v1-release
      ports: 
        - containerPort: 80


    